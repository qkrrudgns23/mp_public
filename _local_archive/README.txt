Local workspace clutter moved from the repo root or `data/Result_storage/` (scratch).

Subfolders:

- logs/ … debug NDJSON or ad-hoc trace files
- profiler/ … cProfile `.pstats` dumps
- result_scratch/ … unreferenced transient sim JSON snapshots

`_local_archive/*` is git-ignored except this file.
