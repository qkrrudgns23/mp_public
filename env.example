# Copy this file to ".env" in the same folder as run_app.py (see .gitignore).
# Or set variables in the OS environment. Layout Design AI uses POST /api/ai-chat.

MOONSHOT_API_KEY=
# Optional overrides:
# MOONSHOT_MODEL=kimi-k2.5
# MOONSHOT_BASE_URL=https://api.moonshot.ai/v1
